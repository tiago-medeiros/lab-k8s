gateway:
  className: envoy-gateway
  namespace: gateway-system
  gatewayName: platform-lab
  protocol: HTTP
  port: 80

cluster:
  name: platform-lab
  dns_domain: cluster.local

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
